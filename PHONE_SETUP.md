# MyGoals on Phone

## Cách dùng ổn nhất

Đưa các file này lên GitHub Pages hoặc Netlify:

- `MyGoals_v3.html`
- `manifest.webmanifest`
- `sw.js`
- `icons/mygoals-icon.svg`

Sau đó mở link trên điện thoại.

## Cài như app

iPhone:

1. Mở link bằng Safari.
2. Bấm Share.
3. Chọn Add to Home Screen.

Android:

1. Mở link bằng Chrome.
2. Bấm menu ba chấm.
3. Chọn Add to Home screen hoặc Install app.

## Đồng bộ đơn giản

Sync tự động giữa máy tính và điện thoại cần cloud database/server. Bản này dùng cách đơn giản hơn:

1. Ở thiết bị đang có dữ liệu mới nhất, mở Dashboard.
2. Bấm Copy Sync.
3. Gửi đoạn sync qua Zalo, Telegram, Google Keep hoặc note riêng.
4. Ở thiết bị còn lại, paste đoạn đó vào ô Sync.
5. Bấm Paste Sync.

## Lưu ý dữ liệu

Dữ liệu được lưu trong trình duyệt của từng thiết bị bằng `localStorage`.

Nếu dùng cả máy tính và điện thoại, hãy dùng Export/Import để backup hoặc chuyển dữ liệu. Không xóa cache/site data nếu chưa export.

## Dùng lâu dài trên một điện thoại

- Dùng Chrome trên Android hoặc Safari trên iPhone, rồi Add to Home Screen.
- Không xóa cache/site data của trình duyệt đang dùng MyGoals.
- Sau khi nhập nhiều dữ liệu quan trọng, bấm Export file hoặc Copy backup.
- Data Vault trên Dashboard sẽ nhắc nếu lâu ngày chưa backup.
- Nếu đổi điện thoại, dùng Copy Sync hoặc Export/Import trước khi xóa máy cũ.
